---
"pi-browser-agent": minor
---

Group each Pi turn into one readable response card, replace internal tool names and production payloads with human-readable activity labels, and add distinct controls for steering or queueing instructions while a run is active.
